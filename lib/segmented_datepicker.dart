library segmented_datepicker;

import 'package:flutter/material.dart';

class SegmentedDatepicker extends StatelessWidget {
  const SegmentedDatepicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('segmented');
  }
}
